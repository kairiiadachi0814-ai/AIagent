# Amazon(atRise・AMS)売上データ表作成

- 出典：Googleドキュメント「Amazon(atRise・AMS)売上データ表作成」
  https://docs.google.com/document/d/1rPz2bY5nEVg_eQNDVV-hSgBhn-k2h8eueCnWLgsW0nM/edit
- 原本最終更新：2025-08-08
- 転記日：2026-07-18
- 責任者照合：未実施（原本と目視で照合すること）
- 注記：
  - クレジットカードの名義・カード名・番号の一部（11002）は広告費集計の特定に必要な情報として原本どおり転記した。ハンドブックに残すかは責任者判断。
  - 未確定の記載（J列:送料自社の入力元、AMS広告費の暫定100万円）は【要確認】に分離した。
  - ID・パスワードの記載なし（認証情報の転記なし）。

※atRise（旧CRE）

## 【作業ファイルの作成】

作業ファイル＜リンク＞：
https://docs.google.com/spreadsheets/u/0/d/1SXpKJIFB8aCN5Cd7tp1erBpY9P7xeqJcrjrf01ujoXA/edit

前月作成のファイルをコピーしてファイル名を変更する。

共有に「足立さん」を追加する（共有情報をそのままコピー）。

※リンクファイルの影響で「マスタ原価」が表示されないので、足立さんに権限付与していただく。

月次税務資料Amazonで取得したトランザクションデータ(csv)…atRise・AMSそれぞれ
（ex.「2024Mar1-2024Mar31CustomTransaction」または「2024MarMonthlyTransaction」）

- →マイドライブへドロップしてアップロード
- →右クリック「アプリで開く」→「スプレッドシート」で開く
- ※前月分の「atRise」「AMS」シートを削除してから！
- →作業ファイルにシートをコピーし、取り込んだシート名を「atRise」「AMS」に変える
  （「集計」シートの式を前月分と変更せずに使えるため）

※「マスタ原価」「原価変換」シートは触ることはない（非表示にしている）。

## 【編集】

### ◎「集計」シート　※赤部分はすべて税込

①A列:日付を変える。

②X列:広告費

- ●AMS ⇒ 計100万円とし、除算を月の日数に修正する
  （いちいち調べにいかずとりあえず100万円を入れている）
- ●atRise　[日次売上実績](https://docs.google.com/spreadsheets/d/16dzOsq4_nUrr95oaopyzatYTiDjn2K0KFNjEIAHh-yg/edit?gid=1825727364#gid=1825727364) の「Amazon広告宣伝費（年月）」に足立さんが日々更新してくださっているので、日別に集計して入れる（税込）
  - クレジットカード：ライズ坂口竜一名義　MARRIOTT BONVOYアメリカン・エキスプレス　11002
  - ご利用内容「Ａｍａｚｏｎ　ｃｏ　ｊｐ　Ｐｒｏｄｕｃ」のデータ
  - →集計は非表示にしている「(広告費集計用)」シートにて

③J列:送料自社 はまた別のところから入れている（後ほど説明してくださる・・・）。

とりあえずは前月分のデータを消しておく。

### ◎「atRise」「AMS」シート

①A列の右に列を挿入→A列を選択→「データ」→「テキストを列に分割」

⇒A列:日付、B列:時間に分割される（必要なのはA列の情報）。

6/7　分割の仕方（区切り方の選択）を聞かれるようになった：

- **A列の右に5列挿入**（2列だけだとデータ列をつぶされる。なぜかは不明）
- →「テキストを列に分割」→「スペース」を指定すると「日付」「時刻」「JST」に分かれる
- →これまでどおりの列数を保つために「JST」の列とデータのない列は削除する
- **⇒C列が「決済番号」になっていればOK**

②P列:商品の売上税 の横に2列挿入→Q列:税込み売上、R列:原価 を作成→数式を入れる。

- ◆Q列:税込み売上 ＝ O列:商品売上 ＋ P列:商品の売上税
- ◆R列:原価 ＝「=IFERROR(VLOOKUP(F9,'原価変換'!$A:$C,3,false),0)」
- 前月分から数式丸ごとコピーでOK
- ※原価0円について・・・広告費用などが原価無し。下記④のマイナス表示のところで計上されている。

数式を入れたら、O列～R列の小数点以下を消し、整数表示にする。

③W列:Amazonポイント ～ AE列:合計 までのマイナスをプラス表示に変える。

編集 → 検索と置換 ⇒「検索：-」「置換後の文字列：何も入れない」で置換。

※AE列は「返金」等のマイナスもまとめてプラスにしているが、今のところ「返金」を集計していないので問題なし。

④表にフィルタをかける。

⇒完成表の最終列がAE列になっていればOK。

## 【「集計」シートの調整】

「集計」シートの数式部分が「エラー　引数は範囲にする必要があります」となっているのは、ダブルクリック→Enterで正しく表示されるので、エラーではない。

⇒前月のシートから式をまとめてコピーして一気に修正。

！！式のコピーなので、誤って値コピーしないこと！！！

！！J列:送料自社、X列:広告費(AMS) はつぶしてはいけないので、他のところのみコピー。

月の日数が31日ない時は、33行目の「計」が正しく表示されるようにエラーを消す。

## 【仕上げ】

「マスタ原価」「原価変換」シートを表示させた場合は非表示にしておく。

## 【要確認】

- J列:送料自社 の入力元・手順が原本に未記載（原本「また別のところから入れている（後ほど説明してくださる・・・）」のまま）。現行の入力手順の確定と本文への追記。
- X列:広告費（AMS）を「計100万円」の固定値で入れる暫定運用（原本「とりあえず100万円を入れている」）が現在も承認された運用か。実額集計に切り替える必要の有無。
